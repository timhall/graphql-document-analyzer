# Kitchen Sink
#
#<- no-space
# <- space
#	<- tab

# anonymous.before
{# a.before.1

	# a.before.2
	a # a.after.1

}# anonymous.after.1
# anonymous.after.2

# B.before
query B(
	# $c.before
	$c:
	# String.before
	String
	# String.after

	d: Int
) {
	# e.before
	e
	# e.selectionSet.before
	{

	}
	# e.selectionSet.after
}
# B.after

mutation F {
	g # g.after
}
